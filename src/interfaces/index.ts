export interface IConfig {
  PORT: number;
  NAME_DB: string;
  USER_DB: string;
  PASSWORD_DB: string;
  HOST_DB: string;
  PORT_DB: number;
  SECRET_HIDDEN_KEY: string;
  PASSWORD_SUPER_ADMIN: string;
}
